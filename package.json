{
  "name": "vivswan-skills-repo",
  "private": true,
  "license": "SEE LICENSE IN LICENSE.md",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "typecheck": "tsc -p .",
    "validate": "bun scripts/validate-skills.ts",
    "smoke": "bun scripts/smoke-test.ts",
    "test": "bun test scripts",
    "test:cli": "bun scripts/cli-discovery-test.ts",
    "lint": "biome ci .",
    "lint:yaml": "uvx yamllint -s .",
    "lint:json": "uvx check-jsonschema --schemafile https://json.schemastore.org/package.json package.json && uvx check-jsonschema --builtin-schema vendor.github-issue-forms .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/feature_request.yml && uvx check-jsonschema --schemafile https://json.schemastore.org/github-issue-config.json .github/ISSUE_TEMPLATE/config.yml",
    "check:fix": "biome check --write .",
    "check": "bun run typecheck && bun run lint && bun run lint:yaml && bun run lint:json && bun run test && bun run validate && bun run smoke"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@types/bun": "^1.3.14",
    "husky": "^9.1.7",
    "typescript": "^7.0.2"
  }
}
