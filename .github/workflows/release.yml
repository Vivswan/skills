# Repo-owned release pipeline, called by the managed ci.yml on top of the
# all-green gate. Generated once by Vivswan/repo-platform and
# never overwritten by template sync.
#
# release-please has been removed from this repository (the module is
# deselected in .repo-platform.yml). This stub only exists because the
# managed ci.yml still contains a `release` job that calls this workflow;
# once a template sync delivers a ci.yml without that job, delete this file.

name: Release

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "release pipeline removed"
