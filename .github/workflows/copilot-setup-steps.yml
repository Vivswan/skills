# Repo-owned setup for the GitHub Copilot coding agent: Copilot runs the
# job below (it MUST be named copilot-setup-steps) to prepare its
# development environment before working on this repository. Triggered on
# edits to itself so changes are validated in CI. Seeded once by the
# fleet sync and never overwritten: add whatever setup the agent needs
# (dependencies, generated files, environment checks).

name: Copilot Setup Steps

on:
  workflow_dispatch:
  push:
    paths: [.github/workflows/copilot-setup-steps.yml]
  pull_request:
    paths: [.github/workflows/copilot-setup-steps.yml]

permissions:
  contents: read

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    timeout-minutes: 59
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
      - name: Install dependencies (bun)
        run: bun install --frozen-lockfile
